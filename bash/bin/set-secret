#!/usr/bin/env bash
# Create a new secret in the keychain.

NAME=$1
SECRET=$2

USAGE="Usage: `basename $0` name secret"

if [[ -z "$NAME" ]] || [[ -z "$SECRET" ]]; then
  echo $USAGE
  exit 1
fi

/usr/bin/security add-generic-password -s Piet -a $1 -w $2 -U
