#!/usr/bin/env bash
# Prune merged branches from local.
set -e

git fetch --quiet

branches="$(git branch --merged | grep -vE '^\*' | grep -vE ' master$' || echo)"

for branch in $branches; do
  git branch -d "$branch"
done
